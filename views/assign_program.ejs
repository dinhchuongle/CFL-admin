<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Gán chương trình học</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-4">
    <h3 class="mb-4">🔗 Gán chương trình học cho lớp</h3>
    <form method="POST" action="/program/assign" class="card p-4 shadow-sm">
      <div class="mb-3">
        <label class="form-label">Lớp</label>
        <select name="className" class="form-select">
          <% classes.forEach(c => { %>
            <option value="<%= c.name %>"><%= c.name %></option>
          <% }) %>
        </select>
      </div>
      <div class="mb-3">
        <label class="form-label">Chương trình học</label>
        <select name="programTitle" class="form-select">
          <% programs.forEach(p => { %>
            <option value="<%= p.title %>"><%= p.title %></option>
          <% }) %>
        </select>
      </div>
      <button class="btn btn-success">Gán chương trình</button>
    </form>
  </div>
</body>
</html>
